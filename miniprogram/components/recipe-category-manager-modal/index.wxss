.category-modal__overlay {
  position: fixed;
  inset: 0;
  z-index: 70;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24rpx;
  box-sizing: border-box;
  background: rgba(18, 28, 45, 0.28);
  backdrop-filter: blur(8rpx);
}

.category-modal {
  width: 100%;
  max-width: 680rpx;
  max-height: 76vh;
  overflow: auto;
  padding: 26rpx;
  border-radius: 34rpx;
  background: rgba(255, 255, 255, 0.98);
  box-shadow: var(--shadow-soft, 0 24rpx 42rpx rgba(37, 49, 71, 0.18));
}

.category-modal__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}

.category-modal__title {
  font-size: 34rpx;
  font-weight: 800;
  color: var(--text-primary, #182234);
}

.category-modal__close,
.category-modal__edit,
.category-modal__delete,
.category-modal__drag {
  width: 56rpx;
  min-width: 56rpx;
  height: 56rpx;
  border-radius: 999rpx;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: 26rpx;
  font-weight: 700;
}

.category-modal__close,
.category-modal__drag {
  background: var(--surface-muted, #f2f4f8);
  color: var(--text-secondary, #667084);
}

.category-modal__input-row {
  display: flex;
  gap: 12rpx;
  margin-top: 18rpx;
}

.category-modal__input {
  flex: 1;
  min-width: 0;
  height: 84rpx;
  box-sizing: border-box;
  padding: 0 22rpx;
  border-radius: 18rpx;
  border: 1px solid var(--border-soft, #edf1f5);
  background: var(--surface-bg, #ffffff);
  font-size: 28rpx;
  color: var(--text-primary, #27323f);
}

.category-modal__confirm {
  width: 184rpx;
  min-width: 0;
  flex: none;
  height: 84rpx;
  border-radius: 16rpx;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: var(--brand);
  color: #ffffff;
  font-size: 30rpx;
  font-weight: 700;
}

.category-modal__hint {
  margin-top: 18rpx;
  font-size: 26rpx;
  color: var(--text-secondary, #7a808d);
}

.category-modal__list {
  margin-top: 18rpx;
  max-height: 52vh;
}

.category-modal__list-inner {
  display: grid;
  gap: 14rpx;
}

.category-modal__item {
  display: flex;
  align-items: center;
  gap: 14rpx;
  padding: 14rpx 16rpx;
  border-radius: 20rpx;
  background: var(--surface-bg, #fbfcfe);
  border: 1px solid var(--border-soft, #edf1f5);
  transition: transform 180ms ease, box-shadow 180ms ease, background-color 180ms ease, border-color 180ms ease;
}

.category-modal__item--dragging {
  transform: translateX(10rpx) scale(1.02);
  background: linear-gradient(135deg, #f3fbf4, #ecf7ef);
  border-color: #b9ddbf;
  box-shadow: 0 16rpx 26rpx rgba(47, 139, 55, 0.18);
}

.category-modal__name {
  flex: 1;
  min-width: 0;
  font-size: 30rpx;
  font-weight: 700;
  color: var(--text-primary, #27323f);
}

.category-modal__actions {
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.category-modal__edit {
  background: var(--surface-muted, #eef5ff);
  color: var(--brand, #4d7edc);
}

.category-modal__count {
  padding: 8rpx 14rpx;
  border-radius: 999rpx;
  background: var(--surface-muted, #f4f5f8);
  color: var(--text-tertiary, #8b95a3);
  font-size: 22rpx;
}

.category-modal__delete {
  background: var(--surface-muted, #ffe9e9);
  color: var(--danger, #d14b4b);
}

.category-modal__drag--active {
  transform: scale(1.08);
  background: #e5f4e7;
  color: #2f8b37;
}
